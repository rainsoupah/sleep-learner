function power(state = [], action) {
  switch (action.type) {
    case 'POWER' :
      // fund invoice
      return state;
    default:
    // all reducers will run
      return state;
  }
}

export default power;
